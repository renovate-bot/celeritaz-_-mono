{
  "name": "celeri-health",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "api": "pnpm dev --filter=nest-api",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "turbo": "^1.12.5"
  },
  "packageManager": "pnpm@8.9.0",
  "engines": {
    "node": ">=20"
  }
}
